module.exports = {
  // ... other config
  themeConfig: {
    navbar: {
      // Hide default navbar if you want to completely replace it
      style: 'dark',
      hideOnScroll: true,
      items: [], // Empty to use custom navbar
    },
    // ... other theme config
  },
}; 