import { LanguageProvider } from './contexts/LanguageContext';

function App() {
  return (
    <LanguageProvider>
      {/* Your existing app code */}
    </LanguageProvider>
  );
}

export default App; 